name: Setup Environment
description: Install Node.js and repository dependencies

inputs:
  node-version-file:
    description: Node.js version file path
    required: false
    default: .nvmrc
  install-mise:
    description: Install mise and tools
    required: false
    default: 'true'
  mise-version:
    description: mise version to install
    required: false
    default: '2026.4.27'
  args:
    description: Arguments passed to `pnpm install`
    required: false
    default: '--frozen-lockfile'
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v6  
      with:  
        cache: true

    - name: Setup Node.js  
      uses: actions/setup-node@v5  
      with:  
        node-version-file: ${{ inputs.node-version-file }}

    - name: Setup mise
      if: inputs.install-mise == 'true'
      uses: jdx/mise-action@v4
      with:
        version: ${{ inputs.mise-version }}
        install_args: --locked

    - name: Install dependencies
      run: pnpm install ${{ inputs.args }}
      shell: bash

    - name: Install Playwright Browsers
      run: pnpm exec playwright install
      shell: bash

    - name: Setup Development Database
      run: pnpm exec tsx scripts/db.ts setup
      shell: bash
